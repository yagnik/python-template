version: '3'

services:
  sampleapp:
    build:
      context: ../
      dockerfile: build/Dockerfile
    volumes:
      - ..:/opt/project
    working_dir: /opt/project
    command: tail -f /dev/null
